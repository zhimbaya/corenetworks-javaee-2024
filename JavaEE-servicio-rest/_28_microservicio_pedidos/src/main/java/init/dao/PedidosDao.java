package init.dao;

import org.springframework.data.jpa.repository.JpaRepository;

import init.model.Pedido;

public interface PedidosDao extends JpaRepository<Pedido, Integer> {
//	List<Pedido> alta(Pedido pedido);
	//save;
//	List<Pedido> pedidos();
	//findAll();
}
